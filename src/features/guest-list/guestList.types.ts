export interface GuestListFormValues {
  name: string;
}

export interface GuestListContent {
  eyebrow: string;
  title: string;
  intro: string;
  formTitle: string;
  formDescription: string;
  nameLabel: string;
  namePlaceholder: string;
  submitLabel: string;
  submittingLabel: string;
  latestTitle: string;
  latestDescription: string;
  latestEmptyState: string;
  successMessage: string;
}
